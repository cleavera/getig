{
  "name": "@getig/common",
  "version": "0.1.0",
  "description": "Common module for getig",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tslint --project ./tsconfig.json",
    "prepack": "git clean -X -f -d --exclude=\"!node_modules\" && npm run lint && npm run build"
  },
  "author": "Anthony Cleaver",
  "license": "ISC",
  "devDependencies": {
    "@getig/core": "^0.4.0",
    "@types/node": "^10.12.15",
    "tslint": "^5.11.0",
    "typescript": "^3.2.2"
  },
  "dependencies": {
    "@cleavera/fs": "^1.1.0",
    "@cleavera/utils": "^2.3.0"
  },
  "peerDependencies": {
    "@getig/core": "^0.4.0"
  }
}
